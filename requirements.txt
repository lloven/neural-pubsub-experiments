# Core dependencies
numpy>=1.24
sentence-transformers>=2.2
openai>=1.0
litellm>=1.0
pandas>=2.0
msgpack>=1.0
fastapi>=0.100
uvicorn>=0.20
httpx>=0.24
pyyaml>=6.0
aiokafka>=0.9

# Development / testing only (not needed in Docker image)
# docker>=6.0
# pytest>=7.0
